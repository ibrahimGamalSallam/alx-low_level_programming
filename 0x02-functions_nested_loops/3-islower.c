#include "main.h"
/**
 *main - islower.c to check if letter is lower or upper case
 */

/**
 * @c: (description of parameter c))*
*/
int _islower(int c)
{
	if  (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	return (0);
}
