#include "main.h"
#include <stdio.h>

/**
 * _isdigit - function to check if the number is a digit
 * @c: parameter to be us
 * Return: always print 0
 */
int _isdigit(int c)
{
	    if (c >= 9)
	    {
		    return (1);
	    }
	    else
	    {
		    return (0);
	    }
}
