#include "main.h"

/**
 * mul - a function to multiply numbers
 * @a: a parmeter to be used
 * @b: a parameters to be used
 * Return: always print 0
 */

int mul(int a, int b)
{
	int sum = a * b;

	return (sum);
}
